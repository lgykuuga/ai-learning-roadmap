services:
  postgres:
    image: postgres:16-alpine
    container_name: roadmap-pg
    environment:
      POSTGRES_USER: roadmap
      POSTGRES_PASSWORD: roadmap123
      POSTGRES_DB: roadmap
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
